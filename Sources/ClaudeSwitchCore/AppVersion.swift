// GENERATED — do not edit by hand.
//
// Stamped by scripts/build-dmg.sh on every build. `marketing` mirrors the
// channel's line in the tracked VERSION file (the single source of truth for
// CFBundleShortVersionString); `build` is the monotonic build number, which is
// what Sparkle actually compares when deciding whether a feed item is newer.
//
// Settings needs both values at compile time, which is the only reason this file
// exists. It decides nothing.
public enum AppVersion {
    public static let marketing = "1.0.0"
    public static let build = "1"
}
